<?xml version="1.0" encoding="utf-8"?>
<!--
  Buttons rest as a quiet outlined surface and fill with the accent when
  focused. Focus is carried by fill, contrast and the card lift together, so it
  never depends on colour alone.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <corners android:radius="@dimen/button_corner" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_raised" />
            <stroke
                android:width="@dimen/hairline_width"
                android:color="@color/hairline" />
            <corners android:radius="@dimen/button_corner" />
        </shape>
    </item>

</selector>

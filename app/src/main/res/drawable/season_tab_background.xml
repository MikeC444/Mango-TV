<?xml version="1.0" encoding="utf-8"?>
<!--
  A season tab has to show two things at once: which season is being shown, and
  where the remote currently is. Focus fills with the accent, as every control
  does; the chosen season keeps a quiet raised surface and an underline, so it
  stays readable while focus is somewhere else.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <corners android:radius="@dimen/button_corner" />
        </shape>
    </item>

    <item android:state_selected="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/surface_raised" />
                    <corners android:radius="@dimen/button_corner" />
                </shape>
            </item>
            <item android:gravity="bottom">
                <shape android:shape="rectangle">
                    <size android:height="2dp" />
                    <solid android:color="@color/accent" />
                </shape>
            </item>
        </layer-list>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/transparent" />
            <corners android:radius="@dimen/button_corner" />
        </shape>
    </item>

</selector>
